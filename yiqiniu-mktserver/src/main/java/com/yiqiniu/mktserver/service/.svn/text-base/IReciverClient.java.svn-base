package com.yiqiniu.mktserver.service;

import java.nio.MappedByteBuffer;

//import com.yiqiniu.core.rtpushserver.client.session.ResponseHolder;

/**
 * <code>IReciverClient</code> 实时接收港股行情数据 客户端接口
 *
 * @author xiongpan
 */
public interface IReciverClient {
	/**
	 * 发送请求
	 * @param sHostAddr
	 * @param sTarget
	 * @param actionType
	 * @param sContent
	 */
	//public void sendRequest(String sHostAddr, String sTarget, int actionType, String sContent);
	/**
	 * @param oRspHolder
	 */
	//public void processResponse(ResponseHolder oRspHolder);
	/**
	 * 启动服务 - 同步
	 */
	public void start();
	/**
	 * 启动服务 - 异步
	 */
	public void asyncStart();
	/**
	 * 停止服务器 
	 */
	public void stop();
	//public MappedByteBuffer getMapBuffer();
	
	//public RandomAccessFile getRandomChannel();
	
	
	/*public void addPacketListener(PacketListener packListener) ;
	
	public void removePacketListener(PacketListener packListener) ;
	
	public List<PacketListener> getPacketListeners() ;*/
	
	
}
